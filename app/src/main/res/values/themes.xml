<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pickable" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Overlay theme for NumberPicker -->
    <style name="ThemeOverlay.NumberPicker" parent="Theme.AppCompat.DayNight">
        <!-- NumberPicker divider color -->
        <item name="colorControlNormal">#B4D6CD</item>
        <item name="android:textColorPrimary">#B4D6CD</item>
        <item name="android:textSize">48sp</item>
    </style>

    <style name="Theme.Pickable" parent="Base.Theme.Pickable" />
</resources>
